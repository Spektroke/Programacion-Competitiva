#include <bits/stdc++.h> 
using namespace std; 
int main(){ 


/* Funcion pasando Parametros por Referencia */
	void sacarEspaciosFinal( string & s ){
		while( s.back() == '' ){
			s.pop_back();
		}
	}
/* Esta funcion recibe un String y lo modifica sacando los '' que 
esten al final del String

/* Funciona que devuelve una Referencia */
	int & incrementar( int & x ){
		x = x+1;
		return x;
	}
/* ______________________________________________________________________________________________
Funcion Recursiva */
	int fibo( int n ){
		if( n<=1){
			return n;
		}
		else{
			return fibo(n-1)+fibo(n-2);
		}
	}
/* La Funcion Fibonacci, el caso Base es si n<=1 y el caso 
recursivo es si n>1
_________________________________________________________________________________________________
/* DIVIDE & CONQUER 
Es la tecnica de dividir un problema en subproblemas mas 
pequeños y hacer la respuesta en base a la de los subproblemas */
/* ______________________________________________________________________________________________
VALORES POR DEFECTO
Podemos hacer que sea posible llamar a una funcion sin pasarle todos
sus parametros, haciendo que estos tengan un valor por defecto 

Ejemplo de Valor por Defecto	*/

	int sumar( vector<int> v, int l=-1, int r=-1 ){
		if( l==-1){
			l=0;
		}if( r==-1){
			r = v.size();
		}
		int suma=0;
		forsn( i,l,r ){
			suma+=v[i];
		}
		return suma;
	}
/* ______________________________________________________________________________________________
PAIR (pareja)
En C++ PAIR nos permite vincular 2 variables de igual o 
distinto tipo y tratarlas como una 
Ejemplos de PAIR

pair<int,int> : 2 Variables enteras
pair<int,string> : 1 Entero y 1 String
pair<string,int> : 1 String y 1 Entero
pair<pair<int,int>, int> : Un par de Enteros y 1 Entero
pair<string, pair<int,int> > Un String y un par de Enteros
*/

pair< int,int > p = {1,2};
pair< string,int > p2 = {"ej",0};
/* p y p2 son el nombre de la pareja */

/* ______________________________________________________________________________________________
TUPLE
En C++ la TUPLA (tuple) generaliza la idea de pair y nos permite juntar varias
variables de igual o distinto tipo juntas

tuple< int,int,int > : 1 Tupla de 3 Enteros
tuple< string, int, int > : 1 Tupla de un String y 2 Enteros
tuple< tuple <int,int>, int > . 1 Tupla de una Tupla de 2 Enteros y 1 Entero
tuple< pair <int,int>, pair <int,int> >: ! Tupla de 2 Pares de Enteros
*/
tuple< int,int,int > t = {1,2,3};

/* Para acceder a un valor de una tupla debemos usar
la función get:		*/
get<ind>(t)

}















